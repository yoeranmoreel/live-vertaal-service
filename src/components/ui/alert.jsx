import React from "react";

export const Alert = (props) => <div {...props}>Alert</div>;
export const AlertDescription = (props) => <div {...props}>AlertDescription</div>;
export const AlertTitle = (props) => <div {...props}>AlertTitle</div>;
